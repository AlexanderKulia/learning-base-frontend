import React from "react";
import { makeStyles, Typography } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({}));

export const NoteEdit = () => {
  return <Typography paragraph>Edit note</Typography>;
};
